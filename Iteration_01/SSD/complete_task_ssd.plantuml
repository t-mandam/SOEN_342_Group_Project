@startuml
title SSD - Complete Task (Success + Failure)

actor User
participant "Personal Task Management System" as System

User -> System: completeTask(taskId)
activate System

alt Task exists AND completable (status == "open")
  System --> User: taskCompleted(taskId)
else Task not found
  System --> User: error("Task not found")
else Not completable (already completed/cancelled)
  System --> User: error("Task cannot be completed")
end

deactivate System
@enduml
