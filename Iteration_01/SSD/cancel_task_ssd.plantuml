@startuml
title SSD - Cancel Task (Success + Failure)

actor User
participant "Personal Task Management System" as System

User -> System: cancelTask(taskId)
activate System

alt Task exists AND cancellable (status not completed/cancelled)
  System --> User: taskCancelled(taskId)
else Task not found
  System --> User: error("Task not found")
else Not cancellable (already completed/cancelled)
  System --> User: error("Task cannot be cancelled")
end

deactivate System
@enduml
