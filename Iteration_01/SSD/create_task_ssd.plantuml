@startuml
title SSD - Create Task (Success + Failure)

actor User
participant "Personal Task Management System" as System

User -> System: createTask(title, description?)
activate System

alt Input valid
    System --> User: taskCreated(taskId)
else Input invalid
    System --> User: error("Invalid task data")
end

deactivate System

@enduml
